const EMPTY_DATABASE = "No URLs in database!";

const NON_EMPTY_DATABASE = "Retrieved All URLs";

const INVALID_URL = "Invalid URL detected!";

const EXISTING_URL = "Existing URL";

const NEW_URL = "New short URL created";

const NO_URL_FOUND = "No long URL found!";

export default {
    EMPTY_DATABASE,
    NON_EMPTY_DATABASE,
    INVALID_URL,
    NEW_URL,
    EXISTING_URL,
    NO_URL_FOUND,
};
